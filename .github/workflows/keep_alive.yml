name: Wake up HF Space
on:
  schedule:
    - cron: '0 0 * * *' # Triggers every day at midnight UTC

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send keep-alive request
        run: curl -s https://hyhlai-mannings-social-tracker-report.hf.space > /dev/null
